<Window x:Class="Учет.Views.RepairHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource ModernWindow}"
        Title="История ремонтов" Height="500" Width="750"
        WindowStartupLocation="CenterOwner">

    <Border Style="{StaticResource ContentCard}">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Border Background="#FF9800" Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                    <TextBlock Text="📋" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock x:Name="TitleText" Text="История ремонтов" FontSize="20" FontWeight="Bold" 
                           VerticalAlignment="Center"/>
            </StackPanel>

            <DataGrid x:Name="RepairsGrid" Grid.Row="2" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      AlternatingRowBackground="#F9F9F9"
                      GridLinesVisibility="Horizontal"
                      BorderBrush="#E0E0E0"
                      BorderThickness="1"
                      Margin="0,0,0,15">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="3" Content="Закрыть" Style="{StaticResource SecondaryButton}" 
                    Click="Close_Click" Width="120" Height="38" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</Window>