<Window x:Class="Учет.Views.DocumentTypeSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource ModernWindow}"
        Title="Выбор типа документа" Height="380" Width="500"
        WindowStartupLocation="CenterOwner">

    <Border Style="{StaticResource ContentCard}">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Border Background="#4CAF50" Width="44" Height="44" CornerRadius="12" Margin="0,0,12,0">
                    <TextBlock Text="📄" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Выберите тип документа" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <ListBox x:Name="TypeList" Grid.Row="2" 
                     BorderBrush="#E0E0E0" BorderThickness="1" 
                     Padding="5"
                     Margin="0,0,0,20">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#757575" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Style="{StaticResource SecondaryButton}" 
                        Click="Cancel_Click" Margin="0,0,12,0" Width="100" Height="38"/>
                <Button Content="Сформировать" Style="{StaticResource PrimaryButton}" 
                        Click="Generate_Click" Width="130" Height="38"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>